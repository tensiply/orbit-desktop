ui/node_modules/
ui/dist/
ui/src/bindings/
src-tauri/target/
target/
.cargo/config.toml

# ts-rs stray export dir when TS_RS_EXPORT_DIR is unset (use `make gen-bindings`)
/src-tauri/bindings/

# Bundled orbit CLI sidecar — downloaded during the release build (see ORBIT_CLI_VERSION)
/src-tauri/binaries/
